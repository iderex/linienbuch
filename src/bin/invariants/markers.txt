# The tool names and generated-by markers, one per line. Blank lines and lines
# beginning with a hash are ignored. Matching ignores case and matches whole
# words only, so an entry inside a longer word is not a match.
#
# This list is data in a file rather than literals in the search's source, for
# the reason tests/environment_guard/needles.txt gives for the same shape: the
# search reads the tracked tree, its own source is in that tree, and a marker
# written there would make the search refuse itself. Excluding the program
# instead would put a hole in exactly the file best placed to hide something.
#
# This file is the one path the search does not read, and that is the whole of
# the hole. A marker anywhere else, including in the program that runs the
# search, is still found.
#
# The list is a floor rather than a boundary. It holds the shapes that credit a
# piece of text to something other than its author. A shape that is not here
# passes, and the entry is added when it arrives.

ai-generated
generated by ai
written by ai
ai assistant
ai language model
chatgpt
copilot

# What is deliberately absent, and the bound this list is honest about.
#
# A product name that is also an ordinary word in this field is not here, and
# will not be added. Two of them are an observatory this board has to cite by
# name and a word for a manuscript, which is what several of the older
# references behind a line list are. Others are people's surnames and appear on
# papers. An entry like that refuses correct text, and a check that refuses
# correct text is one people route around.
#
# So what is searched for above is the shape that credits text to something
# other than its author, plus the two names that are not words in any other
# sense. A bare product name that is also a word is left to the review.
